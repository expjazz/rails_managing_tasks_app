
 <div class="media border d-none" id='chat-default'>


    <div class="media-body">
        <h5 class="mt-0 title font-weight-bold" id='notification'>
          
        </h5>
        <div class="sub-title" id= 'notice-chat'>
          <%# <%= messages_sent %>

          
        </div>

        <%= form_with(scope: :notice, url: './notice_create', method: :post, id: 'chat-form-fluid') do |f| %>
              <div class="form-group">
                 <%= f.label :body %>
                 <%= f.text_field :body, class: 'form-control' %>
               </div>

              <%# <%= hidden_field_tag :chatroom_id, Chatroom.last.id %> %>
              <%# <%= hidden_field_tag :recipient, user.id %> %>


              <%= f.hidden_field :recipient_id, :value => 1 %> 
          <% end %>
      </div>
</div>
